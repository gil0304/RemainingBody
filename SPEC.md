# メディアアート作品 完全仕様書

仮タイトル:『残る身体 / Remaining Body』

## 0. この仕様書について

この仕様書をもとに、展示可能なレベルの高品質なWebベース・メディアアート作品を実装する。
本作品は、カメラの前に立つ鑑賞者の身体をリアルタイムで認識し、その身体の「影」が時間差で空間に残り続けるインタラクティブ作品である。

一般的なWebアプリのようなUIは作らない。ボタン、メニュー、設定パネル、説明ウィンドウ、カードUI、ナビゲーションバーなどは基本的に画面上に表示しない。鑑賞者が作品の前に立った瞬間から、説明なしでも自然に体験が始まることを目標とする。

作品として最も重視するのは:

- 身体の動きの美しさ
- 残像の質感
- 時間の蓄積
- 記憶が薄れていく感覚
- 映像作品としての完成度

「人物検出のデモ」に見えないようにする。

## 1. 作品コンセプト

人は常に現在に存在しているが、その瞬間の身体はすぐに過去になる。この作品では、鑑賞者が動くたびに過去の身体が空間に残る。現在の身体は常に一つしか存在しない。しかし時間が経過するにつれて、数秒前・十数秒前・さらに昔の身体が空間の中に蓄積されていく。最終的に画面の中には「現在の自分」と「多数の過去の自分」が同時に存在する。

## 2. 鑑賞者に感じてほしいこと

説明文を読んでから体験する作品ではなく、身体を動かすことで作品のルールに気づく作品にする。「何か変だ」→「影がついてこない」→「さっきの自分がいる」→「昔の自分がまだ残っている」という順の発見。

## 3. 待機状態

カメラの前に誰もいない場合、画面は静かな状態を維持。完全な黒ではなく、ほぼ黒に近い深い空間(深い黒/ごく薄い霧/微細な粒子/わずかな光の揺らぎ)。目立つアニメーションは不要。スクリーンセーバー的な演出にも見せない。

## 4. 人が入ってきた瞬間

カメラが人物を検知したら自動的に作品が開始。ユーザー操作は不要。「START」「カメラを許可してください」等のUIは展示画面上に表示しない。開発環境ではデバッグ表示を有効化できるようにする。

## 5. 現在の身体

現在の人物はカメラ映像として表示せず、人物のシルエットとして表示。背景は完全に除去。輪郭は精密すぎなくてよく、むしろ若干柔らかい輪郭にする。

## 6. シルエット表現

身体は単純な真っ黒な影ではなく、内側にわずかな階調・ノイズ・粒子・光を持った有機的なシルエット。イメージは煙・墨・水・光・霧・微粒子の中間。

## 7. 現在と過去の区別

現在の身体は最も鮮明。時間が古くなるほど輪郭がぼやける・透明になる・粒子化する・一部が欠損する・ノイズが増える。単純なopacityだけで消さない。

## 8. 残像システム

シルエットを一定間隔で記録。毎フレームではなく 0.6〜1.5秒程度。デフォルト: `snapshotInterval = 900ms`

## 9. 残像の時間

最低でも約30〜60秒は過去の身体を保持。推奨: `maximumShadowAge = 60秒`。古くなるにつれて少しずつ消滅。

## 10. 時間による残像の変化

- 0〜5秒: ほぼ身体の形が残る。透明度も比較的高い。輪郭がはっきり。
- 5〜15秒: 輪郭が少しぼやける。局所的に粒子化が始まる。
- 15〜30秒: 身体の一部が欠け始める。エッジが流れる。ノイズが増える。
- 30〜45秒: 存在の痕跡のようになる。
- 45〜60秒: 粒子や霧に変化しながら消える。

## 11. 残像の動き

過去の身体は完全に固定しない。ごくわずかに漂う・揺れる・崩れる・流れる。ただし激しく動かさない。「映像が動いている」ではなく「記憶が揺らいでいる」と感じる程度。

## 12. 現在の身体の追従

目標遅延 100ms以下、許容 200ms以内。現在の身体だけはレスポンスを良くする。

## 13. 身体認識

ブラウザ上で人物セグメンテーション(MediaPipe / TensorFlow.js / BodyPix / WebGPU対応モデルから、2026年時点で安定・高性能な方式を選択)。顔認識ではなく身体全体のシルエット。個人識別は行わない。

## 14. カメラ

Webカメラ。推奨 1920×1080、30fps以上(可能なら60fps)。セグメンテーションは30fpsで問題ない。

## 15. 複数人対応

可能であれば人物単位で分離。実装負荷が高い場合は全人物をまとめて一枚のマスクとして扱ってよい。複数人の影が混ざることも魅力になり得る。

## 16. 前の鑑賞者の影

鑑賞者が画面から離れても、その人の影をすぐに消さない。「自分が来る前に誰かがここにいた」という感覚を作る。

## 17. 人が誰もいなくなった場合

現在の身体は消えるが残像は残る。残像だけが徐々に崩れ、最後には消える。最後の残像が消えたら待機状態に戻る。

## 18. 長時間展示対応

数時間以上の動作を想定。メモリリーク防止・Canvas/Texture破棄・GPUメモリ管理・古い残像データ削除・配列サイズ制限。残像データを無限に蓄積しない。

## 19. 描画技術

WebGL または WebGPU を基本とする。推奨: Three.js + Custom Shader + WebGL/WebGPU。

## 20. シェーダー

残像表現にShaderを使用。最低限: noise / edge distortion / blur / dissolve / particle breakup。

## 21. Noise

Simplex/Perlin Noise で輪郭を少しずつ崩す。時間によってnoise量を増加。例: `noiseStrength = ageNormalized * 0.08`

## 22. Dissolve

透明度を下げるだけでなく、noise texture を利用した dissolve。身体の一部から徐々に欠けていく。

## 23. Edge

輪郭部分に非常に弱い発光を追加してよい。ただしサイバー/ネオン/SF HUD にはしない。自然で静かな光。

## 24. Particle化

古くなった身体は表面から細かな粒子が離れていく。上方向だけに流さず、ランダムかつゆっくり漂う。

## 25. 背景

基本色 `#050505` 前後。完全な単色ではなく、非常に弱いgradientまたはfilm grain。

## 26. 画面中央を明るくする

非常に弱いradial gradientで画面中央だけわずかに明るく。鑑賞者を自然に中央へ誘導。UIや矢印は使用しない。

## 27. UI

本番画面にはUIを表示しない。禁止: ボタン/ナビゲーション/説明パネル/スライダー/カメラ映像/FPS/デバッグ枠/人物認識ボックス/骨格表示。

## 28. 開発用デバッグモード

`?debug=true` の場合のみ FPS/カメラ映像/Segmentation mask/Detection FPS/Shadow count/GPU情報/解像度/処理時間 を表示可能。通常アクセスでは完全非表示。

## 29. フルスクリーン

展示ではフルスクリーン前提。1920×1080 (16:9)。4Kにも対応できるようレスポンシブに。

## 30. 色設計

基本モノクロ。背景 `rgb(3,3,4)`、現在 `rgb(225,225,230)`、直近の過去 `rgb(150,150,160)`、古い過去 `rgb(50,50,60)`。シェーダー上で微妙な揺らぎを持たせる。

## 31. 色彩オプション

白黒の中にごく微量の青・紫・グレーを混ぜてよい。ただし「青い作品」にはしない。

## 32. 映像の質感

禁止: ゲームっぽい/SFっぽい/サイバーっぽい/UIっぽい/VJ映像っぽい/派手なBloom/激しいGlitch/RGB split/scanline。
目指すのは: 静か・有機的・少し不気味・美しい・儚い。

## 33. 作品開始時

人物が最初に入った瞬間、いきなり100%表示せず、約1〜2秒かけて暗闇から身体が浮かび上がる。

## 34. 人物退出

残像だけが約30〜60秒残り、最後の残像が消えたあと背景だけになり静止状態へ戻る。

## 35. 音

実装する場合も非常にミニマル。必須ではない。BGMは使用しない。環境音のみ(低い空気音/遠くの風/微かな残響/身体が生成されるときの低い音)。

## 36. 動きと音の連動

残像が生まれた瞬間に毎回音を鳴らすのは禁止。一定量の残像が蓄積した場合などに、ごくわずかな音響変化。

## 37. 鑑賞者の動きによる変化

動きが大きい場合、残像が少し崩れやすくなる。動きが小さい場合、残像がより明確に残る。静止/高速移動/手を振る/回転などを試したくなるように。

## 38. Motion Energy

前フレームとの差分から `motionEnergy` を計算、0〜1に正規化。残像の歪み・粒子量・blur・noise に弱く影響させる。

## 39. 静止時

数秒間静止した場合、シルエットが非常に美しく安定する。「止まっていること」にも価値を持たせる。

## 40. 作品の重要な瞬間

鑑賞者が移動したあと、過去の自分の姿がその場所に残っている。これを最も美しく見せる。移動後の状態を重視して映像設計。

## 41. 残像の重なり

単純なalpha blendではなく、少し光が増える/濃度が変わる。ただし白飛びしない。

## 42. 奥行き表現

過去の残像ほど少し奥に存在する感覚(scale/blur/contrast/parallax/noise)。大幅に位置をずらさない。

## 43. カメラ映像反転

鏡として自然に感じられるよう左右反転。

## 44. カメラ映像自体は表示しない

Camera textureは人物抽出のためだけに利用。背景に実写映像を残さない。

## 45. Privacy

画像や映像は保存しない。サーバーへのアップロードもしない。すべてローカルブラウザ内で処理。

## 46. ネット接続

本番展示ではネット接続なしでも動作。AIモデルもローカル配信。CDN依存を避ける。

## 47. 推奨技術構成

Vite / TypeScript / Three.js / WebGL2・WebGPU / MediaPipe / GLSL・WGSL。CSSは最小限。Reactは不要。Vanilla TypeScript推奨。

## 48. プロジェクト構成

src/ 以下に main.ts / app/ / camera/ / segmentation/ / shadow/ / motion/ / rendering/ / shaders/ / debug/ / config/ を分割配置(仕様書の構成例に準拠)。

## 49. ShadowSnapshot

```ts
interface ShadowSnapshot {
  texture: THREE.Texture;
  createdAt: number;
  motionEnergy: number;
}
```

## 50. Shadow age

毎フレーム `age = currentTime - createdAt`、`normalizedAge = clamp(age / MAX_SHADOW_AGE, 0, 1)` としてShaderへ渡す。

## 51. 描画レイヤー

Background → Old Shadows → Recent Shadows → Current Body → Very subtle particles → Post Processing。

## 52. Post Processing

slight bloom / film grain / subtle vignette / very light blur。Bloomは非常に弱く。

## 53. 解像度

内部レンダリング解像度はGPU性能に応じて調整可能に。devicePixelRatio最大1.5。4K展示でも無条件で4K内部レンダリングしない。

## 54. Performance

最低 1920×1080 30fps以上。推奨60fps。

## 55. セグメンテーションFPS

20〜30fpsで問題ない。描画は補間して60fpsを狙う。

## 56. Texture管理

maskのみ低解像度保存 / RenderTarget reuse / texture pooling / compressed mask を検討。

## 57. 最大残像数

`MAX_SHADOWS = 70` 程度。

## 58-62. 展示設営

鑑賞距離2〜4m。カメラはスクリーン中央付近(可能なら上部)。大型ディスプレイまたはプロジェクター(65インチ以上推奨)。プロジェクター時は黒表現重視・暗室。人物側には最低限の照明(スクリーン反射を避ける)。

## 63. エラー処理

カメラが取得できない場合のみ、展示スタッフ用として画面中央に小さく `Camera unavailable` を表示してよい。通常時には絶対表示しない。

## 64. 起動

開発: `npm install` → `npm run dev`。本番: `npm run build` → `npm run preview` またはローカルHTTP Server。

## 65. 開発フェーズ

カメラ取得 → セグメンテーション → シルエット描画 → snapshot保存 → 時間差残像 → ageによるfade → noise/dissolve → particle → motion energy → PostProcessing → 最適化 → 展示モード。

## 66-67. MVPと品質向上

まず カメラ→人物Mask→現在の身体→1秒おきsnapshot→古いsnapshot透明化。その後 Noise/Dissolve/Blur/Particle/Distortion へ段階的に進化。

## 68. 最重要事項

エフェクトを増やすことが目的ではない。常に「過去の身体がそこに残っている」という感覚を最優先。

## 69-70. 方向性

「人が動く→派手なエフェクト→楽しい」はNG。「人が動く→過去の自分が残る→少し不思議→さらに動く→自分が増える→止まって眺める→自分が去る→過去だけが残る」が正。

## 71. 作品としての理想

次の鑑賞者が、自分とは違う誰かの影が残っていることに気づく。「確かに誰かがここにいた」という感覚。この瞬間が最終到達点。

## 72-73. 実装ルール

- ファイルを適切に分割 / main.tsにすべてを書かない / TypeScript / 型を適切に定義
- GPUリソースを明示的にdispose / requestAnimationFrameを適切に管理 / resize対応
- カメラ停止処理 / segmentation停止処理 / long-running展示想定
- console.logを大量に残さない / debug表示はdebug mode限定
- 外部APIに映像を送らない / CDN依存を避ける / 本番ビルド可能な状態を維持

## 74. 完成条件

- 人物検出: 自動認識・全身シルエット・背景非表示
- 現在: リアルタイム追従・低遅延
- 過去: 約1秒ごと記録・空間に残る・新旧が視覚的に区別できる
- 消滅: 古い身体ほど崩れ・dissolve・粒子化・約60秒で消える
- 展示: UIなし・フルスクリーン・長時間起動・ネットなし動作・1920×1080安定
- 表現: スクリーンショットしただけでも一枚の映像作品として美しい

## 75. 最終的な作品体験

暗い画面 → 身体がゆっくり浮かぶ → 動くと影が残る → 過去の自分が増える → 古い自分たちが崩れ粒子になり消える → 鑑賞者が去ると過去だけが残る → 次の鑑賞者の背後に知らない誰かの影。
